# KartPad 0.5.1 source delivery (preparation)

The coordinated delivery identifies compilation source separately from later packaging work. `SOURCE-MANIFEST.json` records the exact clean `sourceRevision`, recursive core `sourceFingerprint`, and input `candidateArtifacts`. Its core snapshot restores that parent and all four maintained runtime pins. The packager checks the complete core fingerprint against the APK and IPA, and the Mac ZIP's embedded source commit, before composing a delivery.

Compilation source is `de3a99c9054042f07e2840066b1bd06f4012659b` for all three
applications, with runtime pins Android `ce9e90aea7682489da6b045e5b2b83f02a0f21f3`,
iOS `cbe43de21dc08e70ed3dea7bd93658ca118986c7`, macOS
`69cdba9143ca7150ccb59e8eb039c8c2870243d3` and tvOS
`686a573744516d594fc7cf5ed1321c46ce34224e`. Later packaging-only commits
must not be described as a new native compilation. Every delivery member is
bound by SHA-256 and size. Source identity and device acceptance are separate.

The dependency sources are retained byte-for-byte from the verified 0.5.0 delivery, which retained its dependency source from 0.4.24. Old prepared application runtimes, old core snapshots and old release scripts are excluded. The original dependency manifests retain their original identities; an old release name in a dependency document does not identify the new app source.

To rebuild:

1. Extract `KartPad-core-source.tar.gz` and use `restore-source-git.py` with `metadata/kartpad.json` to restore the parent and recursive runtime Git identities. Follow the core's Android and Apple build instructions with your own supported game data and the documented SDK/toolchain versions.
2. Use the supplied CMake, Dawn, Maven and Dolphin dependency manifests to locate the accompanying source archives. Their recorded hashes remain authoritative. The profile-tool and Dolphin snapshots include their own restoration metadata.
3. Android uses Dawn base `13abc3bc8ea2d3c2050f9e77a12d012108ceee24` plus both patches under `prototypes/stabilization/dependencies/`. Follow `build-dawn-android.sh`, `pin-dawn-version.py` and `package-dawn-android.py` from the core. The source identity is `b0fd045b0a694eb07ac3fcf0d741f8697b935856`; the verified production binary archive SHA-256 is `c6b4efde6de30f341c46b65a1679914142059b360856d38e32f40dd55cae7fc1`. Populate dependency Git checkouts at the recorded revisions before running the dependency verifier. No external Vulkan validation layer is required.
4. Apple retains its separately pinned Dawn package and build configuration. Do not substitute the patched Android library into an Apple build.
5. Regenerate the private translation/profile inputs from your own game data. Generated game translation, disc images, saves, private traces and signing identities are not included. Use your own signer for installation; it cannot update an app signed by a different identity in place.

The archive carries source inputs and rebuild instructions, not a claim that a different compiler or SDK will produce identical binary bytes. Binary identity checks apply to the exact reviewed candidates. The existing dependency reconstruction documents remain useful for dependency layout; their old application composition descriptions do not describe the current applications, which are rebuilt from the maintained source identified by the delivery manifest.

Release build settings: Android version name 0.5.1, code229, AAB format,
minimum API28; physical iOS version 0.5.1, build85, diagnostics NO; Mac version
0.5.1, build85, Apple Silicon, macOS 14 or later. The private
translation graphs are fingerprinted in each embedded build manifest and must
be regenerated from owned inputs; they are not part of this source delivery.
The source archive does not authorize publication. Publication awaits explicit
owner approval after package audits and an Android public-signer upgrade test.
